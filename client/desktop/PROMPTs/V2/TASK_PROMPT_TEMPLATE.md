# 任务: [在此填写清晰、简洁的任务标题]

## 1. 任务目标 (Task Goal)

> **一句话清晰描述本次任务要达成的最终目标是什么。**
> *例如: “在AI对话页面的顶部工具栏中，实现一个功能完备的模型选择下拉框。”*

## 2. 上下文与关联 (Context & Relations)

*   **所属功能模块**: [例如: `ai_chat`]
*   **关联Feature Prompt**: `[链接到对应的FEATURE_PROMPT.md]`
*   **前置任务**: [如果存在，链接到前置任务的Task Prompt]
*   **后续任务**: [如果存在，链接到后续任务的Task Prompt]

## 3. 任务范围与具体要求 (Scope & Requirements)

> **使用清单(checklist)的形式，详细、无歧义地列出本次任务需要完成的所有具体工作。**

*   [ ] **文件创建/修改**: 
    *   在 `[文件路径]` 中，添加/修改 `[具体类名或方法名]`。
    *   创建新文件 `[文件路径]`，并实现 `[核心类或功能]`。
*   [ ] **UI/UX 实现**: 
    *   实现一个名为 `ModelSelectorDropdown` 的新组件。
    *   该组件必须从 `AIChatController` 的 `models` 列表中获取数据。
    *   当用户选择一个新模型时，必须调用 `controller.selectModel(newModel)` 方法。
    *   组件的视觉风格必须严格遵循 `AppTheme` 和 `LobeTokens` 的规范。
*   [ ] **逻辑实现**: 
    *   在 `AIChatController` 中，实现 `selectModel(String modelId)` 方法，该方法需要更新 `currentModel` 的状态。
    *   当模型切换后，需要将会话的 `system` 提示更新为新模型的默认提示（如果存在）。
*   [ ] **状态管理**: 
    *   `currentModel` 状态必须是响应式的 (`.obs`)。
    *   视图必须通过 `Obx` 或 `GetBuilder` 来监听 `currentModel` 的变化并自动刷新。

## 4. 禁止事项 (Exclusions)

> **明确指出本次任务**不**包含哪些内容，以避免范围蔓延。**

*   本次任务**不**包含模型列表的远程获取逻辑，假定 `models` 列表已由其他逻辑填充。
*   本次任务**不**涉及UI的响应式布局（窄屏适应）。
*   本次任务**不**需要编写单元测试。

## 5. 验收标准 (Acceptance Criteria)

> **定义如何才算“完成”这个任务。标准必须是可验证的。**

*   [ ] 在AI对话页面，顶部工具栏中成功渲染出模型选择下拉框。
*   [ ] 下拉框中正确显示了 `AIChatController.models` 列表中的所有模型ID。
*   [ ] 从下拉框中选择一个新模型后，工具栏上显示的当前模型文本会立即更新为新选择的模型。
*   [ ] 在不重新加载页面的情况下，后续发送的消息会使用新选择的模型进行请求（可以通过打印日志或网络请求来验证）。
*   [ ] 所有新产出的代码都严格遵循项目的编码规范和架构原则。
