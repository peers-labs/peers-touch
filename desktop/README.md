# Desktop Domain

桌面端采用 Tauri + TypeScript + CSS。

## 快速开始

```powershell
cd desktop
npm install
npm run tauri:dev
```

## 命令

```powershell
npm run dev
npm run build
npm run typecheck
npm run test
npm run tauri:dev
npm run tauri:build
```
