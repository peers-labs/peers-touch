{
  "name": "@peers-touch/model",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "gen": "buf generate",
    "lint": "buf lint",
    "breaking": "buf breaking --against .git#branch=main"
  }
}
