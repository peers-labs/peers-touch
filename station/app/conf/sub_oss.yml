peers:
  node:
    server:
      subserver:
        oss:
          enabled: true
          path: /sub-oss
          rds-name: oss
          store-path: ${PEERS_NODE_SERVER_SUBSERVER_OSS_STORE_PATH}
          sign-secret: ""
