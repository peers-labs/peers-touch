package native

import "context"

// initBaseComponents initializes the base components like transport, logger, store for the Top component(like server, registry...)
func (s *native) initBaseComponents(ctx context.Context) error {

	return nil
}
